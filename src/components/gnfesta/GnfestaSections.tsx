/**
 * GnfestaSections — 강릉봄푸드페스타 메인 페이지 (/program/gnfesta) 에서 렌더되는 섹션.
 * 원본 Moosan 프로젝트의 MusanSections (개·폐막식 / 스페셜 게스트 / 기타 프로그램)를
 * 참조해 GNfesta 용으로 재설계 예정. 참조 백업: _DEV/reference/musan_components/
 *
 * 현재는 빌드가 깨지지 않도록 하는 최소 스텁.
 */

interface Props {
  festivalId: string
}

export default function GnfestaSections(_props: Props) {
  return null
}
